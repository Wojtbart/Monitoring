FLASK_SECRET_KEY=zmien_na_losowy_32bajtowy_hex
DATABASE_URL=sqlite:///monitoring.db
JWT_EXPIRES_MINUTES=60
VIDEOS_DIR=videos
# Konfiguracja SMTP przeniesiona do UI: Ustawienia -> SMTP (wysyłka e-mail), zapisywana w bazie (tabela smtp_settings)
